// Valores padrão da aplicação
export const DEFAULT_VALUES = {
  // Meta anual padrão (limite MEI 2025/2026)
  DEFAULT_ANNUAL_TARGET: 81000.00,
  
  // Split PJ padrão (60% empresa, 40% pessoal)
  DEFAULT_SPLIT_PJ: 60,
  
  // Incremento padrão
  DEFAULT_INCREMENT: 0,
  
  // Range da meta (min, max, step)
  TARGET_RANGE: {
    MIN: 10000,
    MAX: 250000,
    STEP: 1
  },
  
  // Range do split PJ
  SPLIT_RANGE: {
    MIN: 0,
    MAX: 100,
    STEP: 5
  }
};
